# OpenClaw directory (default: ~/.openclaw)
OPENCLAW_DIR=~/.openclaw

# Node identity
NODE_NAME=my-machine
ADMIN_PASSWORD=changeme

# Hub connection (leave empty for standalone mode)
NODE_HUB_URL=
NODE_HUB_API_KEY=
NODE_ID=
